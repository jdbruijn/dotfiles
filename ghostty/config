# See https://ghostty.org/docs/config/reference for the available options.

theme = light:Catppuccin Latte,dark:Catppuccin Frappe

# Disable the auto-update functionality as updates will be handled by Homebrew.
auto-update = off

# Increase the scrollback buffer to 100 MiB per terminal surface.
scrollback-limit = 104857600
